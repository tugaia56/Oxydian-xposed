<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Notifica "nuova versione disponibile" del controllo automatico aggiornamenti (WorkManager). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Posizione GPS per il Meteo (SdB/AOD) — richiesta da qui (processo Obsidian), non dal
         Mod: SystemUI stesso non ha ACCESS_FINE_LOCATION (verificato via dumpsys, nessuna riga
         per com.android.systemui), quindi la posizione va presa qui e passata via ObsidianPrefs
         al Mod, esattamente come la città digitata a mano. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".Obsidian"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Obsidian">

        <!-- LSPosed module meta -->
        <meta-data android:name="xposedmodule"       android:value="true" />
        <meta-data android:name="xposeddescription"  android:value="Oxydian customizer for OOS/ColorOS" />
        <meta-data android:name="xposedminversion"   android:value="82" />
        <meta-data android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.AuthActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Obsidian.Transparent" />

        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Safety net: se BOOT_COMPLETED arriva mentre il telefono è ancora bloccato
                     (storage credential-encrypted non leggibile, i prefs letti sotto potrebbero
                     tornare vuoti/default), questo secondo trigger rifà lo stesso lavoro appena
                     l'utente sblocca — stesso pattern di OC (commit bfe28e4e, "USER_UNLOCKED
                     intent for boot reload"). -->
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".ObsidianPreferenceProvider"
            android:authorities="it.tugaia56.obsidian"
            android:directBootAware="true"
            android:exported="true"
            android:multiprocess="false" />

    </application>

</manifest>
